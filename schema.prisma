// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(cuid())
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  createdAt DateTime? @default(now())
}

model Image {
  id              String  @id @default(cuid())
  title           String  @default("")
  alt             String  @default("")
  image_id        String?
  image_filesize  Int?
  image_width     Int?
  image_height    Int?
  image_extension String?
}

model MainPageImage {
  id              String  @id @default(cuid())
  alt             String  @default("")
  image_id        String?
  image_filesize  Int?
  image_width     Int?
  image_height    Int?
  image_extension String?
}

model Project {
  id                 String         @id @default(cuid())
  titleFa            String         @default("")
  titleEn            String         @default("")
  summaryFa          String         @default("")
  summaryEn          String         @default("")
  descriptionFa      String         @default("")
  descriptionEn      String         @default("")
  employerFa         String         @default("")
  employerEn         String         @default("")
  advisorFa          String         @default("")
  advisorEn          String         @default("")
  locationFa         String         @default("")
  locationEn         String         @default("")
  progressPercentage Int?           @default(0)
  status             String?        @default("inProgress")
  startDate          DateTime
  endDate            DateTime?
  images             ProjectImage[] @relation("ProjectImage_project")
}

model ProjectImage {
  id             String   @id @default(cuid())
  alt            String   @default("")
  file_id        String?
  file_filesize  Int?
  file_width     Int?
  file_height    Int?
  file_extension String?
  sortOrder      Int?     @default(0)
  project        Project? @relation("ProjectImage_project", fields: [projectId], references: [id])
  projectId      String?  @map("project")

  @@index([projectId])
}

model Certificate {
  id              String   @id @default(cuid())
  titleFa         String   @default("")
  titleEn         String   @default("")
  image_id        String?
  image_filesize  Int?
  image_width     Int?
  image_height    Int?
  image_extension String?
  date            DateTime
}

model Report {
  id              String   @id @default(cuid())
  titleFa         String   @default("")
  titleEn         String   @default("")
  summaryFa       String   @default("")
  summaryEn       String   @default("")
  descriptionFa   String   @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  descriptionEn   String   @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  date            DateTime
  file_id         String?
  file_filesize   Int?
  file_width      Int?
  file_height     Int?
  file_extension  String?
  relatedReport   Report?  @relation("Report_relatedReport", fields: [relatedReportId], references: [id])
  relatedReportId String?  @unique @map("relatedReport")
  relatedReportOf Report?  @relation("Report_relatedReport")
}

model ReportImage {
  id             String  @id @default(cuid())
  alt            String  @default("")
  file_id        String?
  file_filesize  Int?
  file_width     Int?
  file_height    Int?
  file_extension String?
}

model Award {
  id              String   @id @default(cuid())
  titleFa         String   @default("")
  titleEn         String   @default("")
  image_id        String?
  image_filesize  Int?
  image_width     Int?
  image_height    Int?
  image_extension String?
  date            DateTime
}

model GalleryMedia {
  id            String   @id @default(cuid())
  file_filesize Int?
  file_filename String?
  type          String?  @default("image")
  sortOrder     Int?     @default(0)
  gallery       Gallery? @relation("GalleryMedia_gallery", fields: [galleryId], references: [id])
  galleryId     String?  @map("gallery")

  @@index([galleryId])
}

model Gallery {
  id            String         @id @default(cuid())
  titleFa       String         @default("")
  titleEn       String         @default("")
  descriptionFa String         @default("")
  descriptionEn String         @default("")
  date          DateTime
  medias        GalleryMedia[] @relation("GalleryMedia_gallery")
}

model Comment {
  id        String    @id @default(cuid())
  firstName String    @default("")
  lastName  String    @default("")
  email     String    @default("")
  message   String    @default("")
  createdAt DateTime? @default(now())
}

model Application {
  id              String    @id @default(cuid())
  fullName        String    @default("")
  birthDate       String?
  gender          String?
  marriageStatus  String?
  email           String    @default("")
  resume_filesize Int?
  resume_filename String?
  createdAt       DateTime? @default(now())
}

model Employee {
  id         String @id @default(cuid())
  fullNameFa String @default("")
  fullNameEn String @default("")
  position   String @default("")
}

model Machine {
  id      String @id @default(cuid())
  titleFa String @default("")
  titleEn String @default("")
  count   Int
}
